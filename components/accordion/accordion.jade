.ui.accordion
    div(v-repeat="sections")
        .title(
                v-on="click: toggle($index)",
                v-class="active: isActive($index)"
            )
            content(select=".title") {{* title}}
        .content(v-class="active: isActive($index)")
            content(select=".content") {{* content}}